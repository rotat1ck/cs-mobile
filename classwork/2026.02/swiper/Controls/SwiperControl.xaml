<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="swiper.Controls.SwiperControl"
             >
    <Grid ColumnDefinitions="100,*,100">
        <ContentView x:Name="photo" Padding="40" Grid.ColumnSpan="3" >
            <Grid x:Name="photoGrid" BackgroundColor="Black" Padding="1" 
                  RowDefinitions="*,40">
                <BoxView Grid.Row="2" Color="White"/>
                <Image x:Name="image" Margin="10" Background="#AAAAAA" Aspect="AspectFill"/>
                <Label x:Name="loadingLabel" Text="Loading..." IsVisible="{Binding IsLoading}"
                       HorizontalOptions="Center" VerticalOptions="Center"
                       TextColor="White" FontSize="Large" FontAttributes="Bold" />
                <Label Grid.Row="1" x:Name="descriptionLabel" Margin="10,0" Text="A picture"
                       FontFamily="Bradley Hand"/>
            </Grid>
        </ContentView>

        <StackLayout x:Name="likeStackLayout" Grid.Column="2" Opacity="0" Padding="0,100"
                     HorizontalOptions="Center">
            <Label Text="LIKE" TextColor="Lime" FontSize="30" Rotation="30" FontAttributes="Bold"/>
        </StackLayout>

        <StackLayout x:Name="denyStackLayout" Opacity="0" Padding="0,100" HorizontalOptions="Center">
            <Label Text="Deny" TextColor="Red" FontSize="30" Rotation="-20" FontAttributes="Bold"/>
        </StackLayout>
    </Grid>
</ContentView>